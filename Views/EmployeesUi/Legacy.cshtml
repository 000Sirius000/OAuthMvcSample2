@{
    ViewData["Title"] = "Співробітники (старий інтерфейс)";
}

<h2>Співробітники (старий інтерфейс)</h2>

<table class="table table-striped" id="employeesTable">
    <thead>
        <tr>
            <th>ПІБ</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section Scripts {
    <script>
        // Працюємо з останньою версією API (v2),
        // але використовуємо тільки поля, які були в v1
        fetch('/api/v2/EmployeesApi')
            .then(r => r.json())
            .then(data => {
                const tbody = document.querySelector('#employeesTable tbody');
                data.forEach(e => {
                    const tr = document.createElement('tr');
                    tr.innerHTML = `
                        <td>${e.fullName}</td>
                        <td>${e.email}</td>`;
                    tbody.appendChild(tr);
                });
            });
    </script>
}